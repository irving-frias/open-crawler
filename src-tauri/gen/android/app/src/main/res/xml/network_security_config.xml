<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext HTTP is required for LAN package transfers (the WiFi server on
  `0.0.0.0` speaks plain HTTP so the package can be downloaded without a
  certificate on the receiving device). Traffic to the local network and the
  localhost loopback is allowed; everything else keeps the platform default.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.0.0</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">172.16.0.0</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
</network-security-config>
